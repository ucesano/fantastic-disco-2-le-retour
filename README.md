# Sparse Matrix-Vector Multiplication Investigation

The matrices used are available on SuiteSparse (https://sparse.tamu.edu).

The results are obtained (for strong scaling) via

```bash
./gen_sbatch.sh
```
